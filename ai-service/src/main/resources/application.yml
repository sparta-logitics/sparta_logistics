server:
  port: ${AI_SERVICE_PORT}

spring:
  application:
    name: ai-service
  config:
    import: "configserver:${CONFIG_SERVER_URL}"
  profiles:
    active: local

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_DEFAULT_ZONE}